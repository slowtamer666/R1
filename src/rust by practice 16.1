fn main() {
    let s1 = "hello";
    let s = format!("{}, world!", s1); // Here, we use s1 within the string
    assert_eq!(s, "hello, world!");
}
fn main() {
    print!("Hello world, ");
    print!("I am ");
    print!("Sunface!"); // This will print the full string on the same line
}
